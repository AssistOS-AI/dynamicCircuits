@input leftText rightText
@output total
@invariant totalValid covers total

@left parseNumber $leftText
@right parseNumber $rightText
@total kb.math.sum $left $right
@zero parseNumber "0"
@nonnegative compare $total "ge" $zero
@totalValid assertInvariant $nonnegative "eval.total.nonnegative"
