# Task 2 expectation and observed comparison

All four observations are strictly positive, so the universal claim should be `SUPPORTED`, no witness should be returned,
and the independent arithmetic mean should be 2.5.

The executor produced `SUCCEEDED` with `SUPPORTED`, a null witness, mean 2.5, and receipt
`sha256:ed1055f981e14e2cabf8f82cf502bf5c36fb8d56d426118450d9b57229a6cab2`. The expectation matches the observed result.
