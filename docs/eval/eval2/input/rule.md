# Universal-claim review rule

The claim “all observations are strictly positive” is refuted when any finite observation is less than or equal to zero. A refutation must expose at least one source observation as a witness. A positive mean does not establish the universal claim. Invalid JSON, non-array input, an empty dataset, or a non-finite observation makes the computation unavailable rather than false.
