# Task 3 expectation and observed comparison

Zero is the first non-positive observation in source order, so the universal claim should be `REFUTED` with Z1 as the
witness. The separate mean should be 3.5 and must not rescue the universal claim.

The executor produced `SUCCEEDED` with the expected verdict, witness, mean, and receipt
`sha256:15af7201d9af676681cf019153971333af8c6978e3f80059a8ebd699324a5d38`. The expectation matches the observed result.
